@using System.Reflection
@using Hatdieu.IdentityServer.STS.Identity

@{
    var runtimeVersion = typeof(Startup)
        .GetTypeInfo()
        .Assembly
        .GetCustomAttribute<AssemblyInformationalVersionAttribute>()?.InformationalVersion;
}

@runtimeVersion







